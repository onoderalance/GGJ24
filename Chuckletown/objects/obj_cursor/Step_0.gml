x = mouse_x;
y = mouse_y;

with (obj_customer) {
	target = false;
}

//closestCustomer = instance_nearest(x, y, obj_apple);

//if (distance_to_object(closestCustomer) < range)
//{
//	closestCustomer.target = true;
//}